[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "shepherding"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy", "matplotlib", "torch", "pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
